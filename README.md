# beancode

**First beanforth, then beanmaths, now beancode.** Beancode is a pseudocode transpiler, i.e. it takes in a specific form of pseudocode and generates code in some other language (probably Python, but that could change later).
